[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    unit: pure unit tests (no HTTP client)
    integration: multi-step flows with DB
    api: HTTP API contract tests
    security: CSRF, rate limit, auth hardening
filterwarnings =
    ignore::DeprecationWarning
